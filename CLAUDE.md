# Scapia Skills Marketplace — Authoring Guidelines

Each plugin lives under `plugins/<name>/` and must contain:

```
plugins/<name>/
├── .claude-plugin/
│   └── plugin.json
├── skills/<name>/
│   └── SKILL.md
└── README.md
```
